# 検証記録 — KYOHO 0.1.0

実施日：2026-09-07

## 結果

**機能28項目 + タッチ・データ処理15項目 = 43項目を通過。**

これはテストした範囲の記録であり、製品完成・全端末対応・日本全国の地形精度を保証するものではありません。

環境：Linux上のChromium、Playwright、ANGLE/SwiftShaderのソフトウェアWebGL。ブラウザのネットワーク遷移が制限されているため、生成HTMLをページへ直接設定して検証しました。通常の file URL / HTTP URL によるユーザー環境での起動確認とは区別しています。

### 操作とゲーム進行：28項目

起動、開始ボタン、キーボード移動、キー解放後の減速、ダッシュの速度とエネルギー消費、ジャンプ、長押し滑空、着地、4段階の身長、メニュー中の移動停止、90秒チャレンジ開始と時計、メニュー中の時計停止、8リングの順次回収、結果画面と自己ベスト、タイムアウト、18地点の選択UI、標高RGB復号と無効値、タイル座標の往復、球面座標のkm単位整合、PNG写真生成、保存不可時の説明、JavaScript例外なし、WebGLエラーなし。

### タッチとデータ処理：15項目

タッチによる開始、縦画面の主要操作ボタンの収まり、ブラウザへのタッチイベントでの移動、解放時の停止、タッチでのサイズ変更、横画面の主要操作ボタンの収まり、人工タイルからの日本モード開始、公式URLテンプレート、人工1000mデータの復号、球面地形メッシュの生成、サイズに応じた詳細度変更、人工タイル描画時のWebGLエラーなし、取得失敗の説明、練習島への復帰、JavaScript例外なし。

### テスト方法の補足

入力イベントはブラウザに送り、物理・時計の一部はデバッグ用ステップ関数で時間を進めました。8リングの判定は各リングにプレイヤー位置を設定して状態遷移を検証しており、実際に全コースを走り切るプレイテストと同義ではありません。別の起動確認ではキーを一定時間押して、連続描画中に移動することも確認しています。

テスト用標高PNGは全画素1000mの人工データです。これはRGB復号・非同期読込・メッシュ生成の結合テスト用で、日本のいずれの場所の地形でもありません。ゲーム本体にも同梱していません。実地形として説明したり、宣伝に使ったりしないでください。

## 目視確認と修正

1440×900のPC画面、390×844の縦画面、844×390の横画面を撮影して確認しました。開始ボタンへの装飾要素の重なり、明るい空の上の文字の見づらさ、縦画面での案内テキストとタッチボタンの重なりを修正しました。画面サイズはCSSビューポートで、iPhone実機のスクリーンショットではありません。

## 未検証・既知の制約

- 実際の国土地理院のPNG標高タイルの取得、本当の地形との一致、全国の連続走行。
- iPhone Safari / Android Chrome / PCのハードウェアGPUでの快適性、長時間プレイ、発熱。
- 実ブラウザでのセッションをまたぐlocalStorage保存、IndexedDBキャッシュの永続性。
- OSの共有シート、ファイル保存先の挙動、公開HTTPSサイトからの起動。
- 内陸欠測、海岸線、湖面、タイル境界、詳細度切替時のつなぎ目。
- オンライン対戦、ランキング、住所検索、GPSナビゲーションは実装対象外。

ソフトウェアGPUでの連続描画には低フレームレートの場面がありました。高フレームレートや実機の動作保証は主張していません。

詳細結果は `tests/functional-results.json` と `tests/mobile-data-results.json` に収録しています。
